# Visual Grammar v1 acceptance evidence

This directory proves the SW-008 grammar and template gate.

- `grammar.json` is the canonical `schauwerk-visual-grammar.v1` manifest.
- `validation-receipt.json` records token, state, template and minimum-contrast checks.
- Software evidence under `../lenskit-pilot-20260710/` uses `software-overview-v1`.
- Education evidence under `../education-variants-20260710/` uses `learning-view-v1-rich`.

Both pilots share the same grammar version and accessibility contract while retaining different regions, audiences and output structures. The evidence is static and does not authorize provider mutation.
